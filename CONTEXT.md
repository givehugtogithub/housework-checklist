# 家事打卡表 (Housework Checklist)

一個給 Sean 和 Vera 兩人使用的網頁，用一張月曆表記錄「哪一天、誰做了哪件家事」，月底可以看出兩人分工的多寡。

## Language

**家事項目 (Chore)**:
清單中的一項具體家事（例如洗碗、倒垃圾）。左側欄位，可由使用者新增、刪除、改名。名稱本身就是唯一識別（identity）——新增或改名時若與既有項目同名視為重複，會被擋下。點選名稱文字即可進入編輯狀態改名：Enter 或失焦送出、Esc 取消還原原名；改名不影響該列既有的打勾記錄（同一列的格子資料原封不動保留）。刪除一個家事項目時，該列與其所有格子的紀錄一併消失，不影響其他列。
_Avoid_: 任務、待辦事項

**格子 (Cell)**:
「某個家事」與「某一天」交叉的方格，狀態只會是三選一：空 / Sean 的顏色 / Vera 的顏色。
_Avoid_: 儲存格（表格語境已足夠時不必加此贅字）

**作用中顏色 (Active color)**:
頁面最上方選取的顏色（Sean=藍、Vera=粉），決定接下來點擊格子時要標記成誰的紀錄。初始狀態（頁面剛載入、尚未點選任何一色）沒有作用中顏色，呈現與背景相同的中性色；此時點擊格子不會有效果，格子維持未標記。
_Avoid_: 選色器（那是 UI 元件，這是狀態本身）

**點擊規則**:
點格子時，若格子現有顏色與作用中顏色相同 → 清空該格；若不同 → 直接覆蓋成作用中顏色。

**月曆表 (Month grid)**:
以家事項目為列、以某月每一天（含星期）為欄的表格，本次原型固定顯示 2026 年 9 月，不做月份切換。

**小計 (Tally)**:
依顏色（即依人）加總整月被標記的格子數，顯示「Sean：N 次、Vera：N 次」。
